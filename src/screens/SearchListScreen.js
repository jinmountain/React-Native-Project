import React from 'react';
import { Text, StyleSheet } from 'react-native';

const SearchListScreen = () => {
	return <Text>Search List Screen</Text>
};

const styles = StyleSheet.create({});
export default SearchListScreen;